import hiMom from './hiMom.ts'

// ## Environment
// The environment in Node.js refers to the set of variables that are available to a program at runtime. These variables are stored in the `process.env` object
console.log(process.env, hiMom())
